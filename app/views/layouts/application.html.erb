<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "University of Scouting: GeoCaching Contest" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to root_path, :class=> 'brand' do %> GeoCaching Challenge<% end %>
          <div class="container nav-collapse">

        <% if current_user && current_user.admin? %>
            <ul class="nav">
              <li><%= link_to "Users", users_path  %></li>
              <li><%= link_to "Questions", qandas_path  %></li>           
              <li><a href="reports"> Reports</a> </li>           
            </ul>
        <% end %>

        <% if current_user %>
            <ul class="nav">
              <li><%= link_to "My Answers", posts_path  %></li>
              <li><%= link_to 'Profile', user_path(current_user) %></li>
               <li><%= link_to 'Logout', logout_url  %></li>
            </ul>
        <% end %>

            <ul class="nav pull-right">
              <% if current_user && current_user.admin? %>
              <li class="dropdown">
                <a class="dropdown-toggle" href="#"><%= current_user.email %></a>
                <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                  <li><%= link_to 'Profile', current_user  %></li>
                  <li><%= link_to 'Logout', logout_url  %></li>
                </ul>
              </li>
              <% else %>
              <li><%= link_to "Create", new_user_path  %></li>
              <li><%= link_to "Login", login_path  %></li>
              <% end %>
            </ul>
          </div><!--/.container nav-collapse -->
        </div><!--/.container -->
      </div><!--/.navbar-inner -->
    </div><!--/.avbar navbar-fixed-top -->
<br><br>

    <div class="container">
      <div class="content">
        <div class="row">
           <div class="span12">
              <%= yield %>
            </div>
        </div><!--/row-->
      </div><!--/content-->

      <footer>
      <hr>
        <p>&copy; ScoutMade 2012. All rights reserved.<br>
        <i>"For the benefit of the Scouting movement."</i></p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>