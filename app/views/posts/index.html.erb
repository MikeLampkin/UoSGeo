<h1>My Answer Sheet</h1>
 [FORM WILL GO HERE]

<hr>

<table class="table table-striped table-bordered span3 offset0">
  <tr>
    <th>Question</th>
    <th>Answer</th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td>

<!-- %= post.qanda_id % --> 
<!-- %= image_tag 'meritbadges/'+@post.qandas.try(:image), :height => '40px', :width => '40px'  % -->
<!-- br -->
<!-- %= @post.qanda_id.try(:meritbadge) % -->


    </td>
    <td><%= post.my_answer %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>

  </tr>
<% end %>
</table>

<table class="table table-striped table-bordered">
  <tr>
    <th>Merit Badge</th>
    <th>Question</th>
    <th>Answer</th>
    <th></th>
    <th></th>
  </tr>

<% @qandas.each do |qanda| %>
  <tr>
    <td><%= image_tag 'meritbadges/'+qanda.image, :height => '40px', :width => '40px'  %><br><%= qanda.meritbadge %></td>
    <td><%= qanda.question %></td>
    <td> my answer form </td>
    <td><%= link_to 'Edit', edit_qanda_path(qanda), :class => 'btn btn-mini btn-warning' %></td>
    <td><%= link_to 'Destroy', qanda, :class => 'btn btn-mini btn-danger', method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>